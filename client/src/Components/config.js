// config.js
const api_config = {
  // Set this to localhost if you are running the server locally, other words use the IP address of the server
    API_HOST: "http://localhost",
};

export default api_config;
